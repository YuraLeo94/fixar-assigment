# API URL (optional - auto-detects based on environment)
# Local dev: http://localhost:3000/api (automatic)
# Production: /api (automatic)
#
# Only set this if you need a custom API URL:
# VITE_API_URL=http://localhost:3000/api
